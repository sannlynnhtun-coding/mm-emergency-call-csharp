@page "/"
@inject AuthState AuthState
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(AuthState.IsSignedIn ? "/dashboard" : "/login", replace: true);
    }
}
